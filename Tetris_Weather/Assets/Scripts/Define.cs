using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Define
{
    public enum Scene { None, Lobby, Game, Result };
    public enum GameMode {None, Single, Multi}
    public enum Weather
    {
        rain,
        lightning,

    }
}
